node_modules/
dist/
build/
.DS_Store
*.log
.env
napm.sqlite
.cache/

# Rust / Tauri
/target
/src-tauri/target

# Signing / notarization / updater secrets (M10b) - never commit these
scripts/.notary-config.local
*.p8
*.key

# Original project archive (unpacked into the tree)
napm.zip
