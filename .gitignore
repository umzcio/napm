node_modules/
dist/
build/
.DS_Store
*.log
.env
napm.sqlite
.cache/

# Rust / Tauri
/target
/src-tauri/target

# Signing / notarization / updater secrets (M10b) - never commit these
scripts/.notary-config.local
*.p8
*.key

# Original project archive (unpacked into the tree)
napm.zip

# Playwright MCP scratch output
.playwright-mcp/

# Updater manifest is a release artifact, not committed
/latest.json

# Internal planning artifacts (specs/plans) - kept local, not in the repo
docs/superpowers/
