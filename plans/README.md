# Implementation Plans

Generated by the improve skill on 2026-08-07 against commit `bb85e05`. Execute in
the order below unless dependencies say otherwise.

**Merged (2026-08-08):** PR #7 (plans 001-018 + 026) is **merged to `main`** via rebase.
Note that the rebase rewrote the chain's SHAs, so the per-plan SHAs recorded in the rows
below refer to the pre-merge branches, not to commits on `main`.

The feature work (plans 023 + 024 + 025 + 027) was replayed onto the post-merge `main` as
`advisor/features-023-027` and opened as **PR #8**. The replay was needed because the
rebase-merge of #7 made the feature chain's original base commit no longer an ancestor of
`main`; without it the PR would have shown 57 commits instead of 14. The replay was
conflict-free (the CI fix and the feature work share zero files) and re-verified green.

**Status (2026-08-08):** all 18 code plans (001-018) executed, reviewed, and
approved; stacked on branch `advisor/018-disclosure` (147 tests green) and opened
as **PR #7** (https://github.com/umzcio/napm/pull/7) for review. The 4 design/spike
docs (019-022) are on their own branches. Plans **023-025** are executor-ready
BUILD plans generated from the three greenlit designs; they target the post-PR-#7
codebase and are gated on that PR merging. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

All plans are written for an executor with zero prior context. Verification gate
for every Rust-touching plan: `cd src-tauri && cargo test` (81 tests at planning
time). Frontend changes are verified by running the app (`npm run tauri dev`),
per CONTRIBUTING.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI + verification/format/lint/audit baseline | P1 | S | — | DONE (`advisor/001-ci-baseline` @ e95be2a) |
| 002 | npx version compare + manual sizes (backend quick bugs) | P1 | S | — | DONE (`advisor/002-npx-version-and-manual-size` @ 9963fab) |
| 003 | Store atomicity + duplicate-op rejection (backend) | P1 | M | — | DONE (`advisor/003-store-atomicity` @ 599c836; GUI smoke test pending human QA) |
| 004 | Transfer identity + double-fire guards (frontend) | P1 | M | 003 (soft) | DONE (`advisor/004-transfer-identity` @ 456f670; GUI checklist pending human QA) |
| 005 | CSP + escaping-by-construction | P1 | M | — | DONE (`advisor/005-csp-escaping` @ a9f9591; CSP walkthrough + adversarial check pending human QA) |
| 006 | Version/status logic to Rust, docs made true | P2 | M | 002 | DONE (`advisor/006-version-logic-to-rust` @ fa06ba0; glyph parity reasoned statically, GUI spot-check pending) |
| 007 | Honest error states + one invoke seam | P2 | M | 004, 005 (soft) | DONE (`advisor/007-honest-errors` @ 95c494a; scan/history/search failure UI wired but dormant until backends return Result) |
| 008 | Wire partial-failure honesty | P2 | S | — | DONE (`advisor/008-wire-honesty` @ b801efa) |
| 009 | Parallel scan + memoized discovery | P2 | M | 002, 003 (soft) | DONE (`advisor/009-parallel-scan` @ 5c51f2c; timing check pending human QA) |
| 010 | Registry doc cache + bounded fan-out | P2 | M | — | DONE (`advisor/010-registry-cache` @ 1a2a902; cache-speed check pending human QA) |
| 011 | Render performance (transfers, selection, search tab) | P2 | M | 004 | DONE (`advisor/011-render-perf` @ bd0a5b6; streaming smoothness pending human QA) |
| 012 | Settings file 0600 + password token field | P2 | S | 003 (soft) | DONE (`advisor/012-settings-hardening` @ 1d1cc9e; token rotation recommended, GUI check pending) |
| 013 | Package-name/version validation + strict GitHub URL parse | P2 | S | — | DONE (`advisor/013-input-validation` @ fd386bb) |
| 015 | Release pipeline: jq manifest, bump script, honest update check | P2 | S | — | DONE (`advisor/015-release-pipeline` @ a72de19; updater-modal check pending human QA) |
| 016 | Real command display + Sources-toggle clarity | P3 | S | 004 | DONE (`advisor/016-command-truth` @ 224c04d; row-display check pending human QA) |
| 017 | Lossy UTF-8 streaming + brew catalog single-flight | P3 | S-M | — | DONE (`advisor/017-stream-and-brew-cache` @ 18ffddc) |
| 014 | Manual probe cache/parallelism + preference | P3 | M | — | DONE (`advisor/014-manual-probe` @ 2d495e2; Preferences-toggle check pending human QA) |
| 018 | Network disclosure + advisory-check toggle | P3 | S | — | DONE (`advisor/018-disclosure` @ ac3389e; About/Preferences visual check pending human QA) |
| 019 | DESIGN: one-click uninstall verb | P2* | M | 003, 004 | DONE (design on `advisor/019-uninstall-design`; sign-off pending) |
| 020 | SPIKE: M11 MCP connectors / plugins (read-only) | P3 | S-M | — | DONE (report on `advisor/020-m11-spike` @ 2132cc5, scrubbed + history rewritten after a privacy review flag) |
| 021 | SPIKE: library import manifest (ask maintainer first) | P3 | S (design) | — | DONE (design on `advisor/021-import-design`; build gated on maintainer yes/no recorded in the doc) |
| 022 | DESIGN: cargo as a scanned ecosystem | P3 | M | 009, 013, 018 | DONE (design on `advisor/022-cargo-design`; greenlit → build plan 024) |
| 026 | Get CI green on PR #7 (clippy pin + audit triage) | P1 | S | — | DONE (`advisor/026-ci-green` @ 7bb8db0; fast-forwarded into PR #7, CI re-running) |
| 023 | BUILD: one-click uninstall verb | P2 | M | 026 | DONE (`advisor/023-uninstall` @ fae4106; GUI checklist pending human QA) |
| 024 | BUILD: cargo ecosystem | P3 | M | 023 | DONE (`advisor/024-cargo` @ f8d13d2; live cargo verification pending human QA) |
| 025 | BUILD: library import manifest | P3 | M-L | 024 | DONE (`advisor/025-import` @ 202307f; GUI checklist pending human QA) |
| 027 | Fix raw() concatenation bug in the uninstall modal | P1 | S | 025 | DONE (`advisor/027-raw-concat-fix` @ 21dc86b; defect in 023 found by the 025 executor, verified by differential test) |
| 028 | README catch-up (cargo, uninstall, import) | P2 | S | 023-025 | DONE (merged as PR #9) |
| 030 | Stop the audit job failing on push | P1 | S | — | DONE (merged as PR #11 @ ba2f0d0; confirmed green on the resulting push run to `main`) |
| 032 | Honest display when installed is ahead of the registry | P2 | S-M | — | DONE (merged as PR #12 @ 1069591; 203 tests. GUI check pending human QA) |
| 031 | Shared Library table must fit the window | P1 | M | 032 (integration order only) | DONE (merged as PR #13 @ 8423350; confirmed by the maintainer in a dev build) |
| 033 | Bump script must cover package-lock.json | P2 | S | — | DONE (merged as PR #14 @ f952a13; guard verified in the failing direction) |
| 034 | CSP blocks the app's own font fetch | P1 | S | — | DONE (merged as PR #16; found by a nonce-accurate CSP harness) |
| 035 | Tauri's style nonce disables every inline style attribute | P1 | M | — | DONE (merged as PR #15; the released v0.1.5/v0.1.6 were dimmed and unclickable) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

\* 019 is the highest-leverage direction item but still ranks after the P1 fix plans.

## Standing hazard: the CSP is only enforced in packaged builds

`npm run tauri dev` does NOT apply the `csp` from `tauri.conf.json`, and Tauri appends a style
nonce at runtime which voids `'unsafe-inline'`. Two P1 defects (034, 035) shipped because of this;
035 made the released app dimmed and unclickable. Any change to the CSP, to inline styles, or to a
network call must be checked with a harness that injects the production CSP **plus** `'nonce-x'` in
`style-src` **and** `nonce="x"` on the `<style>` tag. Both halves are required: without the nonce in
the CSP the bug does not reproduce, and without it on the tag the whole stylesheet dies for the
wrong reason. Promoting that harness into the repo as a checked-in smoke test is unplanned and
worth doing.

## Dependency notes

- **001 first.** It turns the 81 existing tests into a gate every other plan's
  done-criteria rely on, and lands the rustfmt baseline before other Rust diffs.
- 004 builds on 003's backend duplicate-rejection as its safety net (either
  order compiles, but 003-then-004 verifies best).
- 006 extends the `scan/version.rs` module 002 creates; do not run 006 first.
- 007 and 011 both restructure frontend functions that 004 and 005 touch;
  execute 004 → 005 → 007 → 011 to keep drift checks clean. 016 follows 004.
- 012 edits the same `write_json` plan 003 rewrites; land 003 first.
- Plans 002/003 vs 009: 009's drift check expects 002/003's diffs; run after.
- Direction plans (019-022) each produce a design/report document and require a
  maintainer decision before any build; 021 explicitly starts with a yes/no.

## Findings considered and rejected (do not re-audit)

- **ureq 2 → 3 migration**: blast radius is one file (`http.rs`), but v2 is
  maintained, nothing drives the move, and `http.rs` is the chokepoint for every
  network feature with no request-level tests. Defer until `cargo audit` (plan
  001) flags it or other work touches `http.rs`.
- **Cross-platform (Linux/Windows)**: already recorded as deferred in
  `docs/ROADMAP.md`; the code-side blockers are small (four `open` calls in
  `lib.rs`) but the real cost is a second signing/updater/packaging story, and
  without CI a second platform would ship unverified. Revisit after 001.
- **npm audit**: clean (dev-only tree, `@tauri-apps/cli`); nothing to do.
- **Global op queue (strictly sequential transfers)**: considered under plan
  003; rejected as a silent product change — only same-package duplicate ops
  are rejected. Revisit if users ask for sequencing.
- **Frontend framework / build step**: against the deliberate single-file
  vanilla design; all frontend plans preserve it. The one recorded tension:
  dropping `withGlobalTauri` (plan 005 notes) would need a build step.
- **`reference/scanner.js` rewrite**: it is documentation of the original CLI;
  plan 015 stops publishing it as a binary, nothing more is warranted.
- **JS lint/typecheck for the inline script**: `tsc` cannot check inline HTML
  scripts; the viable option is `eslint` + an HTML plugin for the typo class
  (`no-undef`). Left as a maintainer tooling decision — record here if adopted
  so a future audit plans it properly.
- **OSV id-prefix severity classification**: documented design decision
  (README "Design Decisions"); not a finding.
- **Brew rollback, pip fuzzy search, npx update paths**: documented honest
  limits; not findings.
- **`package-lock.json` is not bumped by `scripts/bump-version.sh`** (found
  2026-08-08 while reviewing plan 031). The script covers `package.json`,
  `Cargo.toml`, and `tauri.conf.json`, and `release.sh` cross-checks those
  three, but the lockfile still records `0.1.4` while everything else says
  `0.1.5`. Any `npm install` / `npm ci` rewrites it and dirties the tree.
  Not planned yet: two lines in the bump script plus committing the lockfile.
  Do this before the 0.1.6 bump or it recurs.
- **Version cells have no tooltip, so a clipped version is unrecoverable**
  (raised by the 031 executor). Under fixed layout an extreme prerelease like
  `0.15.0-canary.11` ellipsizes; widening the column to fit costs ~40px of the
  Tool column at every width, which is the wrong trade. A `title` on the
  Installed and Latest cells is the cheap fix. Note "Copy tool details" in the
  Edit menu already recovers the full value, so this is a nit, not a defect.
- **Two milder instances of the same "Latest" overclaim**, surfaced by the 032
  executor and deliberately left out of its scope: (a) cargo git/path installs
  set `latest = installed` (`scan/cargo.rs:324-334`), so they print their own
  version under "Latest" as though the registry confirmed it; (b) `libMenu`'s
  disabled "Up to date" label carries the same implication for an `ahead` row.
  Both are honest-display nits, not correctness bugs. Worth one small plan
  together if a third instance appears.
- **What's New badge (116) not matching the status bar's outdated count (113)**:
  correct by construction. `FEED` is alerts + verdicts (`frontend/index.html:572-586`),
  so a security advisory against an already-current package adds a card without
  adding an outdated tool. Not a bug.
- **Not-installed rows in the Shared Library** (`installed: —` with a real
  `latest` and an `Install` button): by design. `InstalledTool.installed` is
  nullable in the canonical data model, and keeping an uninstalled tool visible
  is the point of pins and history. Not a bug.
- **`rustsec/audit-check` failing on push but not on pull_request**: diagnosed
  during plan 030 as informational advisories failing the run. That was wrong.
  The action already excludes informational advisories from its pass/fail count;
  the job failed because the repo's default `GITHUB_TOKEN` is read-only, so the
  Checks API call threw, and the action only swallows that error when
  `GITHUB_HEAD_REF` is set (which GitHub sets for `pull_request` but not for
  `push`). Fixed with a `checks: write` grant. Do not re-audit as an advisory
  problem.

## Audit coverage note

Standard-depth audit at `bb85e05`: full read of `src-tauri/src` (all modules)
and `frontend/index.html`; scripts, configs, docs, and git history reviewed;
`npm audit` run (clean); `cargo audit` NOT run (tool absent — plan 001 adds it
to CI). Not audited: `reference/scanner.js` internals beyond its packaging
implications, icon/asset binaries, and `src-tauri/gen/` (generated).
